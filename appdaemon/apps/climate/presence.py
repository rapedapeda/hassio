def is_present(room_presence, house_presence):
    """
    Determine if the room or house is occupied.

    Args:
        room_presence (str): Room presence sensor ID.
        house_presence (str): House presence sensor ID.

    Returns:
        bool: True if occupied, False otherwise.
    """
    # Placeholder logic for checking presence
    return True
