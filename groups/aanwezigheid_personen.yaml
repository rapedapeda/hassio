# This groups are for presence detection. There should be never used a
# device directly. Instead create a new group.

aanwezigheid_personen:
  name: Aanwezigheid
  entities:
    - person.ramon
    - person.flore
#    - person.sissel
    # Deze zijn specifiek voor HomeKit met automations in iOS Home App
    - input_boolean.tracker_homekit_ramon
    - input_boolean.tracker_homekit_flore