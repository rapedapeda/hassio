- id: snapshot_dagelijks
  alias: Snapshot (dagelijks)
  trigger:
    platform: time
    at: '02:00:00'
  action:
    - service: hassio.snapshot_full
      data:
        name: "{{ now().strftime('%Y-%m-%d') }}-full"
        password: !secret snapshot