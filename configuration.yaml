

config:
energy:
ffmpeg:
frontend:
history: 
media_source:
mobile_app:
person:
stream:
sun:
system_health:
websocket_api:


homeassistant:
  packages: !include_dir_merge_named packages/
  unit_system: metric
  time_zone: Europe/Amsterdam
  country: NL

logger:
  default: info

recorder:
  purge_keep_days: 30
  include:
    domains:
      # Beveiliging
      - alarm_control_panel
      # Klimaat
      - climate
    entities:
      # Energie
      - sensor.irradiance
      - sensor.shellyplug_energy
      - sensor.shellyplug_power
      - sensor.zonnepanelen_power
      - sensor.zonnepanelen_energy
      - sensor.zonnepanelen_radiation
      # Weer (KNMI fallback voor buitentemperatuur)
      - sensor.knmi_temperature
    entity_globs:
      # Energie
      - sensor.p1_*
      # Klimaat
      - sensor.temperatuur_*
      - sensor.luchtvochtigheid_*
      - sensor.luchtdruk_*
      - sensor.luchtkwaliteit_*
      - sensor.licht_*
      # Weer
      - sensor.weerstation*
      # Battery simulator
      - sensor.marstek_venus*
automation: !include_dir_merge_list automations/
input_boolean: !include_dir_merge_named input_boolean/
logbook: !include logbook.yaml


